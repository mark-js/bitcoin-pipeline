sources:
  - name: tick_data
    database: "{{ env_var('GCP_PROJECT_ID') }}"
    schema: "{{ env_var('SOURCE_TICK_DATA_DATASET') }}"
    tables:
      - name: bybit
  - name: onchain_bitcoin
    database: bigquery-public-data
    schema: crypto_bitcoin
    tables:
      - name: blocks
      - name: transactions